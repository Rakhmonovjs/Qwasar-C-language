#include <stdio.h>


int main() {
  int my_index = 0;
 my_index++;
  // replace this comment with an increment
  printf("%d\n", my_index);
   my_index--;
    my_index--;
  // replace this comment with an decrement
  // replace this comment with an decrement
  printf("%d\n", my_index);
   my_index++;
    my_index++;
     my_index++;
  // replace this comment with an increment
  // replace this comment with an increment
  // replace this comment with an increment
  printf("%d\n", my_index);
  return 0;
}